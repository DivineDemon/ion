# Dyck-1 + ION (recurrent). Merge with base + protocol + dyck1 task.
task: dyck1
model: ion
train_min_len: 10
train_max_len: 20
test_lengths: [50, 100, 200]
valid_fraction: 0.5
train_samples: 10000
val_samples: 1000
test_samples_per_length: 500
batch_size: 32
num_workers: 0
input_dim: 1
output_type: classification
num_classes: 2
num_layers: 2
dropout: 0.0
lambda_ind: 0.5
hidden_dim: 64
p_dim: 16
